{
  "name": "gpt-neural-radio",
  "version": "1.0.0",
  "description": "AI Radio — GPT-5.5 neural emotional frequency",
  "type": "module",
  "main": "backend/server.js",
  "scripts": {
    "start": "node --env-file=.env backend/server.js",
    "dev": "node --env-file=.env --watch backend/server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "lowdb": "^7.0.1",
    "node-cron": "^3.0.3",
    "ws": "^8.16.0"
  }
}
